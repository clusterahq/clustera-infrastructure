# Kafka Topics Configuration
# This file defines all Kafka topics created across all environments.
#
# Template Variables:
#   {stack} - Replaced with the current stack name (development, testing, staging, production)
#
# Each topic requires 'name' field. All other fields are optional and override defaults.
# Defaults: partitions=5, replication=2, retention_ms=259200000 (3 days),
#           retention_bytes=5368709120 (5GB), cleanup_policy=delete, compression_type=snappy
#
# Examples:
#   name: "clustera.events"        → clustera.events.development
#   name: "app.commands"           → app.development.commands
#   name: "{stack}-logs"                   → logs-development

topics:
## Integrations
  - name: "{stack}-integrations-inbound-webhook"
  - name: "{stack}-integrations-inbound-trigger"
  - name: "{stack}-integrations-inbound-connection"
  - name: "{stack}-integrations-outbound-data"
  - name: "{stack}-integrations-errors"
  - name: "{stack}-integrations-worker-circle"
  - name: "{stack}-integrations-worker-gmail"
  - name: "{stack}-integrations-worker-google-drive"
  - name: "{stack}-integrations-worker-slack"
  - name: "{stack}-integrations-inbound-webhook-dlq"
  - name: "{stack}-integrations-inbound-trigger-dlq"
  - name: "{stack}-integrations-inbound-connection-dlq"
  - name: "{stack}-integrations-outbound-data-dlq"
  - name: "{stack}-integrations-errors-dlq"
  - name: "{stack}-test-topic"




  # - name: events
  #   # Uses all defaults: 5 partitions, 3-day/5GB retention, snappy compression

  # - name: commands
  #   # Example with custom configuration
  #   partitions: 10
  #   retention_ms: "604800000"  # 7 days
  #   retention_bytes: "10737418240"  # 10GB
