# Kafka Topics Configuration
# This file defines all Kafka topics created across all environments.
#
# Template Variables:
#   {stack} - Replaced with the current stack name (development, testing, staging, production)
#
# Each topic requires 'name' field. All other fields are optional and override defaults.
# Defaults: partitions=5, replication=2, retention_ms=259200000 (3 days),
#           retention_bytes=5368709120 (5GB), cleanup_policy=delete, compression_type=snappy
#
# Examples:
#   name: "clustera.events.{stack}"        → clustera.events.development
#   name: "app.{stack}.commands"           → app.development.commands
#   name: "logs-{stack}"                   → logs-development

topics:
## Integrations
  - name: integrations-inbound-webhook.{stack}
  - name: integrations-inbound-trigger.{stack}
  - name: integrations-inbound-connection.{stack}
  - name: integrations-outbound-data.{stack}
  - name: integrations-errors.{stack}
  - name: integrations-worker-circle.{stack}
  - name: integrations-worker-gmail.{stack}
  - name: integrations-worker-google-drive.{stack}
  - name: integrations-worker-slack.{stack}
  - name: integrations-inbound-webhook-dlq.{stack}
  - name: integrations-inbound-trigger-dlq.{stack}
  - name: integrations-inbound-connection-dlq.{stack}
  - name: integrations-outbound-data-dlq.{stack}
  - name: integrations-errors-dlq.{stack}






  # - name: events
  #   # Uses all defaults: 5 partitions, 3-day/5GB retention, snappy compression

  # - name: commands
  #   # Example with custom configuration
  #   partitions: 10
  #   retention_ms: "604800000"  # 7 days
  #   retention_bytes: "10737418240"  # 10GB
